import React from "react";

const Loading = () => {
  return (
    <div className="w-full min-h-screen flex items-center justify-center">
      <div className="text-bgColor">Loading your drawings...</div>
    </div>
  );
};

export default Loading;
